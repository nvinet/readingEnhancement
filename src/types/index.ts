/**
 * Type definitions index
 * 
 * Centralized exports for all type definitions
 */

export type {
  ReaderConfig,
  ThemeMode,
  ThemeColors,
  Theme,
  ValidationResult,
  FontSizeConstraints,
  SpacingConstraints,
} from './config';
